<!--pages/search_detail/search_detail.wxml-->
  <van-tree-select
    items="{{ items }}"
    main-active-index="{{ mainActiveIndex }}"
    active-id="{{ activeId }}"
    bind:click-item="onClickItem"
    bind:click-nav="onClickNav"
    content-item-class="content-item-class"
  ></van-tree-select>

<view class = "commit">
  <van-button size="large" type="primary" bind:click="confirm"  >确定并返回</van-button>
</view>